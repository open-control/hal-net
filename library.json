{
  "name": "hal-net",
  "version": "0.1.0",
  "description": "Network HAL drivers for Open Control (UDP, WebSocket)",
  "keywords": ["open-control", "network", "hal", "udp", "websocket"],
  "license": "MIT",
  "platforms": ["native"],
  "dependencies": {
    "framework": "*"
  },
  "build": {
    "includeDir": "src",
    "srcFilter": [
      "+<**/*.cpp>"
    ]
  }
}
